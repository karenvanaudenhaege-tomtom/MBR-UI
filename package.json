{
  "name": "mbr-ui",
  "version": "1.0.0",
  "private": true,
  "description": "MBR UI with server-side Anthropic proxy",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
